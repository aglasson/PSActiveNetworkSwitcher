name: CI

on: [push]

jobs:
  lint:
    name: Module linting
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: lint
        run: .github\workflows\lint-scripts\pssa.ps1